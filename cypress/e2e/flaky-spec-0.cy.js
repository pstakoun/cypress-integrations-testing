describe('empty spec', () => {
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
})
