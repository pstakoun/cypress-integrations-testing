describe('empty spec', () => {
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit(Math.random() > 0.5 ? 'https://example.cypress.io' : 'blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('blah', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload!');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn blah');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
})
