describe('empty spec', () => {
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'edit src/app.js and save to reload.');
    cy.get('.app-link').should('have.text', 'learn react');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('yay another one', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
})
