describe('empty spec', () => {
  it('another', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'blah');
    cy.get('.app-link').should('have.text', 'learn react');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'oops');
  });
  it('more failed', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
  it('cra', function() {
    cy.visit('http://localhost:3000');
    cy.get('p').should('have.text', 'Edit src/App.js and save to reload.');
    cy.get('.App-link').should('have.text', 'Learn React');
  });
})
